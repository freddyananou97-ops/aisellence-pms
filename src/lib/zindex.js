/**
 * Centralized z-index system.
 * Import these constants instead of using magic numbers.
 */
export const Z = {
  sidebar: 40,
  dropdown: 50,
  stickyHeader: 60,
  mobileNav: 80,
  mobileMenu: 85,
  modalBackdrop: 100,
  modal: 100,
  toast: 110,
  loginTransition: 9999,
}
